// Declarativas
function myFunction() {
    return 3;
}
// Expresion o Anonymous functions
var myFunctionExpression = function (a, b) {
    console.trace();
    // témplate string - plantilla de cadena de texto
    return `El total es ${a + b}`; // palabra para retornar información
}

console.log(myFunctionExpression(1, 3));
